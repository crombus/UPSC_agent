# Polity-17 Repair Handoff

Reviewed identity `polity-17:learner-v2:g2` is immutable and scored **73/100**.

Repair the Basic, Advanced and canonical Parliament owners; do not edit g2 artifacts. Correct the
Leader of Opposition threshold formulation, Deputy Speaker vacancy date/current control, composition
and session wording, and all stale status dates. Add answer-specific improvement and compression
guidance to all eight PYQ and seven original Mains models. Add exact semantic overrides for all twenty
Core sessions, preserve strict `A → B → C → D`, and rebuild both flow formats as twelve matching Core
routes before optional enrichment.

Generate all four artifacts from one reviewed source ledger into a collision-free immutable successor.
Validate hashes, source identity, prose integrity, MCQ rotation, PYQ metadata, PDF layout and matching
graphical/ASCII coverage. Mark the successor `revalidation_pending`; approval remains false. Never
carry forward g2's score or approval state.
